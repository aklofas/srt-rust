# bindings/c/exports.txt
#
# Apple ld exports list for libtstrans.dylib. Restricts the macOS dynamic
# export table to tst_*/TST_* symbols (Mach-O convention: leading
# underscore prefix on C symbols, so the patterns are _tst_*/_TST_*).
#
# Consumed by build.rs via cargo:rustc-link-arg=-Wl,-exported_symbols_list,...
# on macos-arm64 (phase-in until 2026-05-30).
#
# Apple ld uses positional one-pattern-per-line syntax (no braces, no
# version tags). Linux's version_script.txt uses the parallel ld scheme.

_tst_*
_TST_*
