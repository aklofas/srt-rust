//! C-ABI URL parsing tests for `tst_raw_receiver_*` (plain + managed).
//!
//! Placeholder: receiver-side `_open` entry points accept the same SRT URL
//! grammar (parsed by `tst_srt::SrtUrl::parse`). Dedicated raw-receiver URL
//! roundtrip tests live in `tests/raw_receiver_loopback.rs`; this file is
//! reserved for future URL-specific raw-receiver assertions.
