//! C-ABI URL parsing tests for `tst_receiver_*` (plain + managed).
//!
//! Placeholder: receiver-side `_open` entry points accept the same SRT URL
//! grammar (parsed by `tst_srt::SrtUrl::parse`). Dedicated TS-receiver URL
//! roundtrip tests live in `tests/ts_receiver_loopback.rs`; this file is
//! reserved for future URL-specific TS-receiver assertions.
