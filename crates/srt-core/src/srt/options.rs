//! Typed option wrappers. Filled in Task 3.

pub struct Passphrase;
pub enum KeyLength { Aes128 }
pub enum MaxBandwidth { Unlimited }
pub enum Congestion { Live }
pub struct StreamId;
pub struct PacketFilter;
