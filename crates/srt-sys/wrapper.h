/* wrapper.h — single header consumed by bindgen for libsrt's public API.
 *
 * Including srt.h alone is sufficient; transitive headers (logging_api.h,
 * platform_sys.h, etc.) are pulled in by srt.h itself.
 */

#include <srt/srt.h>
