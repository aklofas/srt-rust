[package]
name        = "tst-test-helpers"
version     = "0.0.0"
edition     = "2024"
rust-version = "1.85"
publish     = false
description = "Internal test helpers shared across tst-core, tst-pipeline, tst-srt. Not for external consumption."
license     = "MIT OR Apache-2.0"

[dependencies]
# Filled in by Tasks 8/9/10 as each helper module lands. Likely:
#   tst-core = { path = "../tst-core" }   (mock_transport implements Transport)
